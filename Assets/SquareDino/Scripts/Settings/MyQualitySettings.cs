using UnityEngine;

namespace SquareDino.Scripts.Settings
{
    // using UnityEditor.Presets;
    public class MyQualitySettings : MonoBehaviour
    {
        // public Preset qualitySettings;
        
        public void ApplySettings()
        {
            // var preset = Instantiate(settings);
            // qualitySettings.ApplyTo();
            // Preset.SetAsDefault(qualitySettings);
        }
    }
}